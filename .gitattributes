# Normalize line endings to LF in the repo, regardless of OS (standard for web projects)
* text=auto eol=lf

# Explicitly binary — never touch line endings
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
*.woff binary
